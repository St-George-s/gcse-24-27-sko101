# Happy birthday Muskaan!

# (Function)
# Length and breadth are parameters.
def getAreaRectangle(length, breadth):
    return length * breadth

# Call a subprogram
print(getAreaRectangle(10, 20))

returnedValue = getAreaRectangle(10, 30)
print(returnedValue)

secondReturnedValue = (getAreaRectangle(5, 4))
print(secondReturnedValue)

if returnedValue > secondReturnedValue:
    print("First area is bigger. ")